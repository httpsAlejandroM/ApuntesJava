package interfaces;

public interface Comunicable {


    //La clase que implemente esta interfaz estará obligada a usar este metodo
    //Las interfaces pueden tener atributos tipo final (constantes), o publicas o statics pero no pueden tener getters y setters
    //tampoco pueden tener constructores (las clases abstractas si)
    void comunicarse();
}
